#!/usr/bin/node
// variable defined
const text = ['C is fun', 'Python is cool', 'JavaScript is amazing'];
// initialize variable
let i = 0;

while (i < text.length) {
  console.log(text[i]);
  i++;
}
